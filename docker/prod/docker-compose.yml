services:
  app:
    build:
      context: ../..
      dockerfile: docker/prod/Dockerfile
    container_name: vue-prod
    volumes:
      - prod_modules:/app/node_modules
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  prod_modules:
    name: prod_modules